package com.example.jpa.jpa;

public class JpaMainTestForIsJpaUpdateOrInsertWhenRecordWithIdIsExistedInDB {

	// TODO: check if whether JPA perform insert or update a record when that record has
	// primary key id same as in DB
	// TODO: check as the same above but the ID is not the primary key
	public static void main(String[] args) {
		
	}
	
}
